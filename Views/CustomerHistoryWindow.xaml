<Window x:Class="SnookerGameManagementSystem.Views.CustomerHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Customer History" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a2e">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowBackground" Value="#0f3460"/>
            <Setter Property="AlternatingRowBackground" Value="#16213e"/>
            <Setter Property="RowHeight" Value="40"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#0f3460"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a2a45"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsInitialCredit}" Value="True">
                    <Setter Property="Background" Value="#2d1f3f"/>
                    <Setter Property="FontStyle" Value="Italic"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding CustomerName}" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      Foreground="White"
                      Margin="0,0,0,5"/>
            <TextBlock Text="Game History" 
                      FontSize="12" 
                      Foreground="#a0a0b0"/>
        </StackPanel>

        <!-- Summary Stats -->
        <Border Grid.Row="1"
               Background="#16213e"
               BorderBrush="#3a3a4e"
               BorderThickness="2"
               CornerRadius="8"
               Padding="20"
               Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Games" Foreground="#a0a0b0" FontSize="12"/>
                    <TextBlock Text="{Binding TotalGames}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Foreground="#a0a0b0" FontSize="10" Margin="0,2,0,0">
                        <Run Text="W:"/>
                        <Run Text="{Binding TotalWins}" Foreground="#4caf50"/>
                        <Run Text=" L:"/>
                        <Run Text="{Binding TotalLosses}" Foreground="#e94560"/>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Win Rate" Foreground="#a0a0b0" FontSize="12"/>
                    <TextBlock Text="{Binding WinRateDisplay}" FontSize="20" FontWeight="Bold" Foreground="#4caf50"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Initial Credit" Foreground="#a0a0b0" FontSize="12"/>
                    <TextBlock Text="{Binding InitialCredit, StringFormat='PKR {0:N2}'}" FontSize="20" FontWeight="Bold" Foreground="#9c27b0"/>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="Total Charged" Foreground="#a0a0b0" FontSize="12"/>
                    <TextBlock Text="{Binding TotalCharged, StringFormat='PKR {0:N2}'}" FontSize="20" FontWeight="Bold" Foreground="#e94560"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Total Paid" Foreground="#a0a0b0" FontSize="12"/>
                    <TextBlock Text="{Binding TotalPaid, StringFormat='PKR {0:N2}'}" FontSize="20" FontWeight="Bold" Foreground="#4caf50"/>
                </StackPanel>

                <StackPanel Grid.Column="5">
                    <TextBlock Text="Balance Due" Foreground="#a0a0b0" FontSize="12"/>
                    <TextBlock Text="{Binding BalanceDue, StringFormat='PKR {0:N2}'}" FontSize="20" FontWeight="Bold" Foreground="#ff9800"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- History DataGrid -->
        <DataGrid Grid.Row="2"
                 Style="{StaticResource DataGridStyle}"
                 ItemsSource="{Binding GameHistory}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date/Time" Binding="{Binding Timestamp, StringFormat='dd/MM/yyyy HH:mm'}" Width="130"/>
                <DataGridTextColumn Header="Table" Binding="{Binding TableName}" Width="90"/>
                <DataGridTextColumn Header="Game Type" Binding="{Binding GameType}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInitialCredit}" Value="True">
                                    <Setter Property="Foreground" Value="#9c27b0"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="90"/>
                <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Result}" Value="Win">
                                    <Setter Property="Foreground" Value="#4caf50"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Result}" Value="Lose">
                                    <Setter Property="Foreground" Value="#e94560"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Win Streak" Binding="{Binding WinStreak}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding WinStreak}" Value="0">
                                    <Setter Property="Foreground" Value="#666666"/>
                                    <Setter Property="Text" Value="-"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Result}" Value="Win">
                                    <Setter Property="Foreground" Value="#4caf50"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Charged" Binding="{Binding TotalCharged, StringFormat='PKR {0:N2}'}" Width="100"/>
                <DataGridTextColumn Header="Paid" Binding="{Binding AmountPaid, StringFormat='PKR {0:N2}'}" Width="100"/>
                <DataGridTextColumn Header="Due" Binding="{Binding AmountDue, StringFormat='PKR {0:N2}'}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#ff9800"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AmountDue}" Value="0">
                                    <Setter Property="Foreground" Value="#4caf50"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Status" Binding="{Binding PaymentStatus}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PaymentStatus}" Value="Paid">
                                    <Setter Property="Foreground" Value="#4caf50"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding PaymentStatus}" Value="Unpaid">
                                    <Setter Property="Foreground" Value="#e94560"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding PaymentStatus}" Value="Partial">
                                    <Setter Property="Foreground" Value="#ff9800"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Method" Binding="{Binding PaymentMethod}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#a0a0b0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PaymentMethod}" Value="-">
                                    <Setter Property="Foreground" Value="#555555"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
