<Window x:Class="SnookerGameManagementSystem.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Snooker Club Dashboard" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#1a1a2e">

    <Window.Resources>
        <!-- Session Tile Style -->
        <Style x:Key="SessionTile" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <ImageBrush ImageSource="/Images/snooker-table-bg.jpg" Stretch="Fill"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="5" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#e94560" BlurRadius="20" ShadowDepth="5" Opacity="0.7"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Add Table Button Style -->
        <Style x:Key="AddTableButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <ImageBrush ImageSource="/Images/snooker-table-bg.jpg" Stretch="Fill"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="5" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                ClipToBounds="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#e94560"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#e94560" BlurRadius="20" ShadowDepth="5" Opacity="0.7"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0f3460" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Logo and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎱" Foreground="White" FontSize="50" FontFamily="Segoe UI Emoji" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="SNOOKER CLUB" 
                                  FontSize="20" 
                                  FontWeight="Bold" 
                                  Foreground="White"/>
                        <TextBlock Text="Active Tables Dashboard" 
                                  FontSize="12" 
                                  Foreground="#a0a0b0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Right: Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Button Content="🔄 Refresh" 
                           Command="{Binding RefreshCommand}"
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           Margin="0,0,10,0"
                           BorderThickness="0"
                           Cursor="Hand"/>
                    
                    <Button Content="🎮 Game Types" 
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           Margin="0,0,10,0"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding GameTypesCommand}"/>
                    
                    <Button Content="📊 Reports" 
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           Margin="0,0,10,0"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding ReportsCommand}"/>
                    
                    <Button Content="👥  Customers" 
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding CustomersCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="20">
            <!-- Wrap Panel containing both Add button and session tiles -->
            <WrapPanel Orientation="Horizontal">
                <!-- Add Table Button (First in WrapPanel) -->
                <Button Style="{StaticResource AddTableButton}"
                       Command="{Binding AddTableCommand}"
                       Width="320"
                       Height="200"
                       Margin="12">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="+" FontSize="72" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text="Add Table" FontSize="16" HorizontalAlignment="Center" Foreground="White" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <!-- Session Tiles -->
                <ItemsControl ItemsSource="{Binding Sessions}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SessionTile}"
                                   Width="320"
                                   Height="200"
                                   MouseLeftButtonUp="SessionTile_Click">
                                <!-- Content centered on the table background -->
                                <Grid Margin="48,34,48,34">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Table Name -->
                                    <TextBlock Grid.Row="0"
                                              Text="{Binding Name}"
                                              FontSize="15"
                                              FontWeight="Bold"
                                              Foreground="White"
                                              HorizontalAlignment="Center"
                                              TextTrimming="CharacterEllipsis"
                                              Margin="0,0,0,3">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.9"/>
                                        </TextBlock.Effect>
                                    </TextBlock>

                                    <!-- Game Type -->
                                    <TextBlock Grid.Row="1"
                                              Text="{Binding GameTypeName}"
                                              FontSize="11"
                                              Foreground="#FFD700"
                                              HorizontalAlignment="Center"
                                              FontWeight="Bold"
                                              TextTrimming="CharacterEllipsis"
                                              Margin="0,0,0,4">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.9"/>
                                        </TextBlock.Effect>
                                    </TextBlock>

                                    <!-- Players Display -->
                                    <TextBlock Grid.Row="2"
                                              Text="{Binding PlayersDisplay}"
                                              FontSize="9"
                                              Foreground="#FFFFFF"
                                              HorizontalAlignment="Center"
                                              TextWrapping="Wrap"
                                              TextAlignment="Center"
                                              TextTrimming="CharacterEllipsis"
                                              MaxHeight="20"
                                              FontWeight="SemiBold"
                                              Margin="0,0,0,4">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.9"/>
                                        </TextBlock.Effect>
                                    </TextBlock>

                                    <!-- Timer -->
                                    <Border Grid.Row="3"
                                           Background="#1a1a2e" 
                                           BorderBrush="#FFD700" 
                                           BorderThickness="2" 
                                           CornerRadius="4" 
                                           Padding="8,3"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.8"/>
                                        </Border.Effect>
                                        <TextBlock Text="{Binding ElapsedTimeDisplay}"
                                                  FontSize="15"
                                                  FontWeight="Bold"
                                                  Foreground="#FFFFFF"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Frame Count -->
                                    <Border Grid.Row="4"
                                            Background="#1a1a2e"
                                            BorderBrush="#FFD700"
                                            BorderThickness="1"
                                            CornerRadius="3"
                                            Padding="6,2"
                                            HorizontalAlignment="Center">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="2" Opacity="0.8"/>
                                        </Border.Effect>
                                        <TextBlock FontSize="9"
                                                  Foreground="#FFFFFF"
                                                  HorizontalAlignment="Center"
                                                  FontWeight="Bold">
                                            <Run Text="Frames:" Foreground="#a0a0b0"/>
                                            <Run Text=" "/>
                                            <Run Text="{Binding FrameCount, Mode=OneWay}" FontWeight="ExtraBold" Foreground="#FFD700" FontSize="10"/>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.Row="1"
               Background="#80000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading..."
                          FontSize="20"
                          Foreground="White"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
