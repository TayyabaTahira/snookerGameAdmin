<Window x:Class="SnookerGameManagementSystem.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Snooker Club Dashboard" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#1a1a2e">

    <Window.Resources>
        <!-- Session Tile Style -->
        <Style x:Key="SessionTile" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#e94560"/>
                    <Setter Property="Background" Value="#0f3460"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Add Table Button Style -->
        <Style x:Key="AddTableButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="#a0a0b0"/>
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#e94560"/>
                    <Setter Property="Foreground" Value="#e94560"/>
                    <Setter Property="Background" Value="#0f3460"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0f3460" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🎱" Foreground="White" FontSize="50" FontFamily="Segoe UI Emoji" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="SNOOKER CLUB" 
                                  FontSize="20" 
                                  FontWeight="Bold" 
                                  Foreground="White"/>
                        <TextBlock Text="Active Tables Dashboard" 
                                  FontSize="12" 
                                  Foreground="#a0a0b0"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="🔄 Refresh" 
                           Command="{Binding RefreshCommand}"
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           Margin="0,0,10,0"
                           BorderThickness="0"
                           Cursor="Hand"/>
                    
                    <Button Content="🎮 Game Types" 
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           Margin="0,0,10,0"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding GameTypesCommand}"/>
                    
                    <Button Content="📊 Reports" 
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           Margin="0,0,10,0"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding ReportsCommand}"/>
                    
                    <Button Content="👥  Customers" 
                           Background="#16213e"
                           Foreground="White"
                           FontFamily="Segoe UI Emoji"
                           Padding="15,8"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding CustomersCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="20">
            <!-- Wrap Panel containing both Add button and session tiles -->
            <WrapPanel Orientation="Horizontal">
                <!-- Add Table Button (First in WrapPanel) -->
                <Button Style="{StaticResource AddTableButton}"
                       Command="{Binding AddTableCommand}"
                       Width="250"
                       Height="200"
                       Margin="10">
                    <StackPanel>
                        <TextBlock Text="+" FontSize="72" HorizontalAlignment="Center" Margin="0,20,0,10"/>
                        <TextBlock Text="Add Table" FontSize="16" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Session Tiles -->
                <ItemsControl ItemsSource="{Binding Sessions}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SessionTile}"
                                   Width="250"
                                   Height="200"
                                   MouseLeftButtonUp="SessionTile_Click">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Table Name -->
                                    <TextBlock Grid.Row="0"
                                          Text="{Binding Name}"
                                          FontSize="20"
                                          FontWeight="Bold"
                                          Foreground="White"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,15"/>

                                <!-- Info -->
                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding GameTypeName}"
                                              FontSize="14"
                                              Foreground="#e94560"
                                              HorizontalAlignment="Center"
                                              Margin="0,0,0,10"/>
                                    
                                    <TextBlock Text="{Binding PlayersDisplay}"
                                              FontSize="12"
                                              Foreground="#a0a0b0"
                                              HorizontalAlignment="Center"
                                              TextWrapping="Wrap"
                                              TextAlignment="Center"
                                              Margin="0,0,0,10"/>
                                    
                                    <TextBlock Text="{Binding ElapsedTimeDisplay}"
                                              FontSize="18"
                                              FontWeight="SemiBold"
                                              Foreground="White"
                                              HorizontalAlignment="Center"/>
                                </StackPanel>

                                <!-- Frame Count -->
                                <TextBlock Grid.Row="2"
                                          FontSize="12"
                                          Foreground="#a0a0b0"
                                          HorizontalAlignment="Center">
                                    <Run Text="Frames:"/>
                                    <Run Text="{Binding FrameCount, Mode=OneWay}" FontWeight="Bold"/>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </WrapPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.Row="1"
               Background="#80000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading..."
                          FontSize="20"
                          Foreground="White"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
