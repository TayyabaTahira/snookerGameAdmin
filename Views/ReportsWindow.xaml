<Window x:Class="SnookerGameManagementSystem.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reports &amp; Analytics" 
        Height="900" 
        Width="1400"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a2e"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4caf50"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a0a0b0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="RowBackground" Value="#16213e"/>
            <Setter Property="AlternatingRowBackground" Value="#1a1a2e"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="RowHeight" Value="40"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Hidden ScrollBar Style -->
        <Style x:Key="HiddenScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="0"/>
            <Setter Property="Opacity" Value="0"/>
        </Style>
        
        <!-- Custom DatePicker Style with hover effect -->
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,0,15,0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="BorderElement"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox"
                                                  Grid.Column="0"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  FontWeight="{TemplateBinding FontWeight}"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Padding="15,0,5,0"
                                                  VerticalContentAlignment="Center"/>
                                <Button x:Name="PART_Button"
                                       Grid.Column="1"
                                       Width="35"
                                       Height="35"
                                       Margin="0,0,5,0"
                                       Background="Transparent"
                                       BorderThickness="0"
                                       VerticalAlignment="Center">
                                    <Path Data="M19,4H5A2,2 0 0,0 3,6V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V6A2,2 0 0,0 19,4M19,20H5V10H19V20M19,8H5V6H19V8Z"
                                          Fill="#555"
                                          Stretch="Uniform"
                                          Width="20"
                                          Height="20"/>
                                </Button>
                                <Popup x:Name="PART_Popup"
                                      AllowsTransparency="True"
                                      Placement="Bottom"
                                      PlacementTarget="{Binding ElementName=PART_TextBox}"
                                      StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BorderElement" Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="BorderElement" Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly">
        <ScrollViewer.Resources>
            <Style TargetType="ScrollBar" BasedOn="{StaticResource HiddenScrollBar}"/>
        </ScrollViewer.Resources>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock FontSize="28" 
                              FontWeight="Bold" 
                              Foreground="White">
                        <Run Text="📊" FontFamily="Segoe UI Emoji"/>
                        <Run Text=" Reports &amp; Analytics"/>
                    </TextBlock>
                    <TextBlock Text="Revenue, games, and player statistics" 
                              FontSize="14" 
                              Foreground="#a0a0b0"
                              Margin="0,8,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="From:" 
                              Foreground="White"
                              FontSize="14"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding FromDate}"
                               Style="{StaticResource DatePickerStyle}"
                               SelectedDateFormat="Short"/>
                    
                    <TextBlock Text="To:" 
                              Foreground="White"
                              FontSize="14"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="20,0,10,0"/>
                    <DatePicker SelectedDate="{Binding ToDate}"
                               Style="{StaticResource DatePickerStyle}"
                               SelectedDateFormat="Short"/>
                </StackPanel>
            </Grid>

            <!-- Statistics Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Today's Revenue / To Date Revenue -->
                <Border Grid.Column="0" Style="{StaticResource StatCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource LabelText}" 
                                  FontWeight="SemiBold">
                            <Run Text="💰" FontFamily="Segoe UI Emoji"/>
                            <Run Text=" "/>
                            <Run Text="{Binding TodayRevenueLabel, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Text="{Binding TodayRevenue, StringFormat='PKR {0:N2}'}" 
                                  Style="{StaticResource ValueText}"
                                  Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding TodayGamesLabel}" 
                                  Style="{StaticResource LabelText}"
                                  Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- This Month / Selected Range Revenue -->
                <Border Grid.Column="1" Style="{StaticResource StatCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource LabelText}" FontWeight="SemiBold">
                            <Run Text="📅" FontFamily="Segoe UI Emoji"/>
                            <Run Text=" "/>
                            <Run Text="{Binding RangeRevenueLabel, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Text="{Binding RangeRevenue, StringFormat='PKR {0:N2}'}" 
                                  Style="{StaticResource ValueText}"
                                  Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding RangeGamesLabel}" 
                                  Style="{StaticResource LabelText}"
                                  Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Outstanding Credit -->
                <Border Grid.Column="2" Style="{StaticResource StatCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource LabelText}" FontWeight="SemiBold">
                            <Run Text="⚠️" FontFamily="Segoe UI Emoji"/>
                            <Run Text=" Outstanding Credit"/>
                        </TextBlock>
                        <TextBlock Text="{Binding OutstandingCredit, StringFormat='PKR {0:N2}'}" 
                                  Style="{StaticResource ValueText}"
                                  Foreground="#e94560"
                                  Margin="0,8,0,0"/>
                        <TextBlock Text="Unpaid balances" 
                                  Style="{StaticResource LabelText}"
                                  Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Average per Game -->
                <Border Grid.Column="3" Style="{StaticResource StatCard}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource LabelText}" FontWeight="SemiBold">
                            <Run Text="📈" FontFamily="Segoe UI Emoji"/>
                            <Run Text=" Avg per Game"/>
                        </TextBlock>
                        <TextBlock Text="{Binding AveragePerGame, StringFormat='PKR {0:N0}'}" 
                                  Style="{StaticResource ValueText}"
                                  Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding AverageLabel}" 
                                  Style="{StaticResource LabelText}"
                                  Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Details Section -->
            <Grid Grid.Row="2" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Top Players -->
                <Border Grid.Column="0" Style="{StaticResource StatCard}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Style="{StaticResource HeaderText}">
                            <Run Text="🏆" FontFamily="Segoe UI Emoji"/>
                            <Run Text=" Top Players (Date Range)"/>
                        </TextBlock>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="450">
                            <DataGrid ItemsSource="{Binding TopPlayers}"
                                     Margin="0" Padding="10">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Games" Binding="{Binding GamesPlayed}" Width="70"/>
                                    <DataGridTextColumn Header="Won" Binding="{Binding GamesWon}" Width="60"/>
                                    <DataGridTextColumn Header="Win %" Binding="{Binding WinRateDisplay}" Width="70"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

                <!-- Recent Games -->
                <Border Grid.Column="1" Style="{StaticResource StatCard}" Margin="12,0,0,0">
                    <StackPanel>
                        <TextBlock Style="{StaticResource HeaderText}">
                            <Run Text="🎮" FontFamily="Segoe UI Emoji"/>
                            <Run Text=" Recent Games (Date Range)"/>
                        </TextBlock>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="450">
                            <DataGrid ItemsSource="{Binding RecentGames}"
                                     Margin="0" Padding="10">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                                    <DataGridTextColumn Header="Players" Binding="{Binding Players}" Width="2*"/>
                                    <DataGridTextColumn Header="Winner" Binding="{Binding Winner}" Width="*"/>
                                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="110"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Loading Overlay -->
            <Border Grid.RowSpan="3" 
                   Background="#AA000000" 
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock FontSize="20" 
                              Foreground="White"
                              HorizontalAlignment="Center">
                        <Run Text="⏳" FontFamily="Segoe UI Emoji"/>
                        <Run Text=" Loading Reports..."/>
                    </TextBlock>
                    <ProgressBar IsIndeterminate="True" 
                                Width="200" 
                                Height="4"
                                Margin="0,20,0,0"
                                Foreground="#e94560"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Window>
