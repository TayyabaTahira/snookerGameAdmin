<Window x:Class="SnookerGameManagementSystem.Views.TableManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Table Management"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a2e"
        ResizeMode="CanResize"
        Icon="/Images/snooker_logo.png">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="AddButton" TargetType="Button">
            <Setter Property="Background" Value="#4caf50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45a049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TableCard" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#5A8FA0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#5A8FA0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4D6A92"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#ff6b6b"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e94560"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Style="{StaticResource HeaderText}">
                    <Run Text="Table Management"/>
                </TextBlock>
                <TextBlock Text="Manage physical tables for your snooker club - Drag to reorder"
                          FontSize="14"
                          Foreground="#a0a0b0"
                          Margin="0,8,0,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                   Content="+ Add Table"
                   Style="{StaticResource AddButton}"
                   Command="{Binding AddTableCommand}"
                   VerticalAlignment="Center"/>
        </Grid>

        <!-- Tables List -->
        <ScrollViewer Grid.Row="1"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Tables}"
                         AllowDrop="True">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource TableCard}"
                               AllowDrop="True"
                               PreviewMouseLeftButtonDown="TableCard_PreviewMouseLeftButtonDown"
                               MouseMove="TableCard_MouseMove"
                               Drop="TableCard_Drop"
                               DragOver="TableCard_DragOver"
                               DragLeave="TableCard_DragLeave"
                               Tag="{Binding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Table Info -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                             FontSize="18"
                                             FontWeight="Bold"
                                             Foreground="White"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <!-- Status Badge -->
                                        <Border Style="{StaticResource StatusBadge}"
                                               Background="{Binding StatusColor}"
                                               Margin="0,0,10,0">
                                            <TextBlock Text="{Binding StatusBadge}"
                                                     FontSize="12"
                                                     FontWeight="SemiBold"
                                                     Foreground="White"/>
                                        </Border>

                                        <!-- Active Status -->
                                        <Border Style="{StaticResource StatusBadge}"
                                               Background="#3a3a4e"
                                               Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="Active"
                                                     FontSize="12"
                                                     FontWeight="SemiBold"
                                                     Foreground="White"/>
                                        </Border>
                                        
                                        <Border Style="{StaticResource StatusBadge}"
                                               Background="#808090"
                                               Visibility="{Binding IsActive, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                            <TextBlock Text="Inactive"
                                                     FontSize="12"
                                                     FontWeight="SemiBold"
                                                     Foreground="White"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- Session Info -->
                                    <StackPanel Orientation="Horizontal"
                                               Margin="0,8,0,0"
                                               Visibility="{Binding ShowSessionInfo, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="{Binding GameTypeName}"
                                                 FontSize="13"
                                                 Foreground="#a0a0b0"
                                                 Margin="0,0,15,0"/>
                                        <TextBlock FontSize="13" Foreground="#a0a0b0">
                                            <Run Text="{Binding PlayerCount, Mode=OneWay}"/>
                                            <Run Text="Players"/>
                                        </TextBlock>
                                        <TextBlock Text=" - "
                                                 FontSize="13"
                                                 Foreground="#a0a0b0"
                                                 Margin="5,0"/>
                                        <TextBlock FontSize="13" Foreground="#a0a0b0">
                                            <Run Text="{Binding FrameCount, Mode=OneWay}"/>
                                            <Run Text="Frames"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1"
                                           Orientation="Horizontal"
                                           VerticalAlignment="Center"
                                           Margin="20,0,0,0">
                                    <Button Content="Edit"
                                           Style="{StaticResource ActionButton}"
                                           Command="{Binding DataContext.EditTableCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                           CommandParameter="{Binding}"/>
                                    <Button Content="Delete"
                                           Style="{StaticResource DeleteButton}"
                                           Command="{Binding DataContext.DeleteTableCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                           CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2"
               Background="#AA000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock FontSize="20"
                          Foreground="White"
                          HorizontalAlignment="Center">
                    <Run Text="⏳" FontFamily="Segoe UI Emoji"/>
                    <Run Text=" Loading Tables..."/>
                </TextBlock>
                <ProgressBar IsIndeterminate="True"
                            Width="200"
                            Height="4"
                            Margin="0,20,0,0"
                            Foreground="#5A8FA0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
